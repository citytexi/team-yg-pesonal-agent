---
tags: [lint, maintenance]
updated: 2026-07-31
---

# Lint 보고서 2026-07-31

기계 검사 `python3 wiki/script/lint.py`(68건) + `python3 wiki/script/check-status.py`(소스 36건) 실행.
직전 보고서 [[lint-2026-07-29]] 이후 **위키·raw 변경 0건**(이 기간 작업은 전부 `parfait/` 구현 문서 쪽).
따라서 이번 회차는 기계 검사 + 구현 delta(TJYG-Android `develop` #165)의 위키 하류 반영 확인이 실질 범위.

## 민감 데이터 ⚠️
- 없음 (lint.py 민감 패턴 위반 0건)

## 기계 검사 결과
- **위반 0건** (frontmatter·링크 무결성·의존 방향·raw 정합·출처 추적·status 전 항목)
- **참고 14건** — 전부 기존 알려진 유형, 조치 불필요:
  - 깨진링크 참고 4건: [[conventions]]·`log.md`의 템플릿 플레이스홀더(`[[파일명]]`·`[[개념명]]`·`[[소스]]`·`[[대체본]]`)
  - 의존방향 참고 4건: `log.md`·`wiki/CLAUDE.md`·[[lint-2026-07-06]]의 `parfait/` 링크 — 정책 콘텐츠 페이지가 아니라 이력·스키마·과거 보고서라 규약 대상 밖
  - 약연결 6건: [[conventions]]·[[overview]]·lint 보고서 4건(인바운드 = index 1건) — 성격상 정상

## 모순 → open-questions.md 등록됨
- 신규 모순 0건. 위키 문서 간 새 상충 없음(원본 유입이 없었다).

## 해소분 하류 반영
- 직전 회차([[lint-2026-07-29]])에서 해소 처리한 항목들의 `⚠️` 마커·개념 페이지 반영은 그때 완료됨. 재확인 결과 잔존 없음.
- **이번 회차 갱신 1건** — [[nametag-chip]] 타입 개수 상충(open-questions [2026-07-18]):
  TJYG-Android 구현이 `NametagChipPlus`의 용도를 **멤버 5명 이상일 때의 "+" 집계 칩**(색 타입 아님)으로
  확정 → 쟁점이 "정책 12종 vs 코드 14종"에서 **"정책 12종 vs 코드 13종"**으로 좁혀졌다.
  [[nametag-chip]] `⚠️` 마커와 [[open-questions]] 항목을 함께 정정(상충 자체는 **미해결 유지**).
  구현 심볼 `YGColorChipType`은 이름·변형 불변이라 위키 서술의 다른 부분은 그대로 유효하다.

## 고아 페이지
- 없음 (인바운드 0건 페이지 없음)

## 링크 없는 언급
- 없음

## 판본 상태(status)
- `check-status.py` 소스 36건 검사, **위반 0건 / 경고 0건**. index 투영 불일치 없음.

## 데이터 공백 / 조사 권고
직전 회차에서 넘어온 5건, **모두 잔존**(신규 원본 유입이 없어 변동 없음):
1. [[g-001-empty-툴팁]] — 툴팁 **문구·앵커 미정**([[G-001-Empty-툴팁-노출-조건-정책-v0.1]]이 노출·닫기 조건만 규정).
2. [[토핑]] 조회 실패 그래픽 — 물음표 1종의 **실 에셋 미확보**(에셋 시트는 Template01~06 + Template-E까지).
3. `G-001-토핑-정책 v0.2` **미수집** — 실재 확인됨([[G-001-무한파르페-정책설계-v0.3]] §8.2 참조), 수집 시 [[G-001-토핑-정책-v0.1]]의 `superseded_by` 정정 필요.
4. [[이름-입력-규칙]] — **금칙어·그룹 내 닉네임 중복** 정책 미결.
5. 참조명 불일치 2건 — Empty 툴팁 원문 참조명 vs 실제 파일명, [[S-102-그룹-닉네임-생성-정책-v0.1]]이 참조하는 `S-002-…-v0.3`(실재하지 않는 판본).

추가 관찰(구현 쪽에서 넘어올 예정):
- Grouptag-Chip **그레이 타입 타임스탬프 색** — 구현(Figma) `Gray-200` vs 위키 [[nametag-chip]] ② 표 `White` 상충이
  Android 구현 문서에 등록돼 있다. 해당 코드가 `develop` 미머지 상태라 **위키 등록은 머지 시점으로 보류**(작업자 판단 유지).
  머지되면 [[open-questions]] 항목 + ② 표 `⚠️` 마커가 등록 대상.

## stale 정보
- 수정 대상 없음. 직전 회차에서 7건 정리 후 원본 유입이 없어 새 stale이 생기지 않았다.
