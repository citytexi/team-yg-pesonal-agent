---
tags: [lint, maintenance]
updated: 2026-07-27
---

# Lint 보고서 2026-07-27

> 대상: `wiki/` 전체(55개 md, `personal-private/` 제외 — private 서브모듈). 계기: [[G-001-무한파르페-간격-정책-v0.3]] ingest 직후.

## 민감 데이터 ⚠️
- **0건**. 이메일·전화번호·API 키/토큰/비밀번호 패턴 탐지 0.
- 실명 매핑 대상 11명(`personal-private/담당자-매핑.md` 기준) 노출 `wiki/`·`raw/` 통틀어 **0건**.
  - v0.3 원본 표지 작성자 실명 → `DSN-B` 마스킹 확인. 이미지 속 그룹 닉네임(잠탈감금 등)은 정책 본문 미포함.

## 모순 → open-questions.md 등록됨
ingest 단계에서 2건 등록(신규), 이번 lint 추가 감지 **0건**.

- [[G-001-무한파르페-간격-정책-v0.3]] §2.2 내부: Right 열 y 서술("인접 두 Left 가운데" = 342) vs 수치(354 = Left+86) 불일치 → **미해결**.
- [[G-001-무한파르페-간격-정책-v0.3]] §2.2 vs §2.4: N=3 첫 토핑 y가 268인지 280인지 미확정 → **미해결**.

## 고아 페이지
- **0건**.

## 깨진 링크
- 실질 **0건**. 탐지된 17개는 전부 스키마 템플릿 플레이스홀더(`[[페이지A]]`·`[[파일명]]`·`[[개념명]]` 등)와 과거 lint 보고서가 인용한 parfait 측 페이지(`[[data-layer]]`·`[[design-system]]`·`[[lint-2026-07-06-parfait]]`) — 위키 스코프 밖. 이전 lint와 동일 판정.

## raw ↔ sources 정합
- **0건**. `raw/*.md` 29건 ↔ `wiki/sources/*.md` 1:1 대응, 누락·잉여 없음.
- index 등록 누락 **0건** (sources·concepts·entities 전건 `[[링크]]` 등록).

## stale 정보 → 수정 완료 (4건)
v0.3 승격에 따라 "v0.2 = 정본/현행" 잔존 표기를 정정.

| 파일 | 내용 |
|---|---|
| [[G-001-무한파르페-간격-정책-v0.1]] | "정본은 v0.2" → 현행 정본 v0.3(직전본 v0.2) |
| [[G-001-무한파르페-정책설계]] | 배치 모델 대체처에 v0.3 현행 정본 명시 |
| [[G-001-무한파르페-정책설계-v0.2]] | "배치 상수 상세 = v0.2 보유"에 v0.3(좌표·인셋 확정) 추가 |
| `wiki/index.md` | frontmatter `updated` 2026-07-25 → 본문 표기(2026-07-27)와 정합 |

- 위 4건 + 간격정책 v0.2 sources의 `updated`를 2026-07-27로 갱신.

## 데이터 공백 / 조사 권고
- **G-001 좌표 미확정 2건**(위 open-questions) — 구현 착수 전 디자인 확인 필요. 현재 코드 기준은 수치값(Right 354/502/650) 권장.
- **N=0 온보딩 툴팁 `G-001-Empty`**: v0.3에서 안내 주체로 확정됐으나 툴팁 자체의 문구·노출 조건 정책 소스 없음 → 별도 정책서 필요.
- **크림 가로 길이 1안/2안** 미정 잔존([[open-questions]] 2026-07-06 항목 ②), **체리 기능 TBD**(③) — v0.3에서도 미언급.
- [[G-001-토핑-정책-v0.1]] raw가 여전히 구값(180 프레임·3타입) → v0.2/v0.3 후속본 필요([[open-questions]] 2026-07-22 항목).

## 결론
**차단 이슈 0건**(민감데이터·고아·깨진 링크·정합 모두 0). stale 4건은 이번 lint에서 수정 완료. 커밋 가능.

---

# 2차 점검 — 판본 상태(status) 스키마 도입 후

> 같은 날 2회차. 계기: 리뷰 지적 3건(맥락 미구분 인용 / 폐기본 미표시 / 미결 상태 매칭 누락)을
> 스키마에 반영(커밋 `4f05b22`)한 뒤 전면 재점검. 대상 `wiki/` 56개 md(`personal-private/` 제외).

## 민감 데이터 ⚠️
- **0건**. 패턴 6종 스캔(email·휴대폰·API key/secret/token·JWT·주민번호·`/Users/` 절대경로) 전건 무탐지.

## 판본 상태(status) 무결성 — 신규 점검 항목 7
- `python3 wiki/script/check-status.py` → **소스 28건, 위반 0건**.
- 검사: 필드 누락 / 값 오류 / `superseded`·`partial`의 `superseded_by`·`scope` 누락 / 깨진 참조 /
  `superseded_by` 체인의 `current` 종단(사이클·끊김) / 역방향 정합 / index 투영 불일치.
- 분포: `current` 17 · `superseded` 10 · `partial` 1([[C-001-캔버스-정책-v0.1]] — Blur 존폐만 미결).

## 고아 페이지
- **0건** (56건 전수 인바운드 링크 검사).

## 깨진 링크
- 실질 **0건**. 탐지 52개는 전부 ⓐ `wiki/CLAUDE.md`·`open-questions.md`의 템플릿 플레이스홀더,
  ⓑ 과거 lint 보고서가 인용한 parfait 페이지(`[[data-layer]]` 등, 위키 스코프 밖). 이전 판정 유지.
- **살아있는 페이지(concepts·entities·sources·index·overview) 깨진 링크 0건.**

## raw ↔ sources 정합
- 1:1 대응 **28 ↔ 28**, 누락·잉여 0건. `sources:` 프론트매터가 가리키는 raw 파일 전건 실재.
- ⚠️ **파일명 유니코드 정규화 불일치 1건** — `raw/C-001-캔버스-정책-v0.1.md`만 **NFD**(자모 분리),
  나머지 27건은 NFC. git 인덱스는 NFC로 저장돼 있어 **커밋·diff엔 영향 없으나**, 디스크 경로를
  NFC 문자열로 매칭하는 스크립트·검색이 이 파일만 못 찾는다(이번 스캔에서 실제로 "raw만 있음 +
  sources만 있음" 동시 오탐 발생). → **미수정**(raw 불변 원칙). 처리 여부 사용자 확인 대기.

## stale 정보 → 수정 완료 (6건)
status 도입으로 **폐기본을 현행 근거로 인용한 곳**이 드러남. 규격 자체는 동일해 내용 오류는 아니나,
"`current` 아닌 소스는 현행 근거로 인용 금지" 규칙 위반.

| 파일 | 수정 |
|---|---|
| `overview.md` | 토핑 컴포넌트 "v0.2 현행" → **v0.3 현행**(규격은 v0.2에서 무변경 명시) |
| [[그룹]] | 조회 근거 [[G-001-무한파르페-정책설계]](v0.1 폐기본) → **[[G-001-무한파르페-정책설계-v0.2]]** |
| [[nametag-chip]] | Grouptag-Chip 노출 토핑 규격 v0.2 → **[[G-001-무한파르페-간격-정책-v0.3]]** |
| [[open-questions]] Nametag 항목 출처 A | S-101 v0.2 → **v0.3**(12종은 무변경) |
| [[open-questions]] Nametag 항목 해소 메모 | 갱신 대상 S-101 v0.2 → **v0.3** |
| [[open-questions]] 토핑 버전정합 해소 메모 | "코드/디자인은 v0.2 기준" → **v0.3 기준** |

## 데이터 공백 → 마커 보강 (3곳)
미결이 `open-questions.md`에만 있고 개념 페이지에서 안 보이던 곳에 마커·링크 추가.

- [[화면-ID-체계]] — G-002 삭제 후 **권한 검증 이동처 미명시** ⚠️ 추가.
- [[화면-ID-체계]] — v6 비고 **"에딧 모드 삭제" vs C-301~C-306 잔존** ⚠️ 추가.
- [[무한-파르페-그리드]] — 크림 가로 길이 미정 · 체리 기능 TBD에 [[open-questions]] 링크 추가.

## 미결 현황 (부정 매칭 기준)
14항목 중 **해소됨 4 / 미결 10**. 이 중 `부분 해소 (①④⑤ 해소·무효, ②③ 잔존)` 1건은
기존 "미해결" 문자열 매칭에서 **누락되던 항목** — 이번 스키마 변경으로 미결 집계에 포함됨.

## 링크 없는 언급
- 파르페 요소(크림 9 / 체리 14 / 접시 8 / 인디케이터 8 파일 언급) — 페이지 생성 **임계 미달** 판정.
  [[무한-파르페-그리드]] §파르페 요소가 z-index·개수·고정 갭을 이미 커버, 단독 재참조 수요 낮음.
  단 미결 2건(크림 가로·체리 기능)이 확정되면 재검토.

## 개선 권고 (검사 자체)
- 이번에 시험한 "폐기본 인용" 스캔은 40건 탐지 중 **실질 6건**(나머지는 명칭 변천·버전 이력·
  open-questions 출처 A/B 등 정당한 과거 인용). `check-status.py`에 편입하려면 **이력·출처 문맥
  제외 규칙**이 먼저 필요 — 현 상태로 자동화하면 오탐이 신호를 덮는다. 이번엔 수동 판별로 처리.

## 결론
**차단 이슈 0건.** stale 6건·마커 공백 3곳 수정 완료. NFD 파일명 1건만 사용자 판단 대기(커밋 영향 없음).
